@model WebAttendance.Models.ViewModels.AllowedPersonsToRegisterViewModel

<h1>Три имена</h1>

<div asp-validation-summary="All">   </div>
<form method="post" asp-controller="UsersAlloewdToRegister" asp-action="EditAPtoReg">
    <input type="hidden" asp-for="@Model.AllowedPersonsToRegister.Id" />
    <div class="border p-3">
        <div class="form-group row">
            <h2>Моля редактирайте</h2>
        </div>
        <div class="row">
            <div class="col-12">
                <div class="form-group row">
                    <div class="col-4">
                        <label asp-for="@Model.AllowedPersonsToRegister.Name"></label>
                        <input asp-for="@Model.AllowedPersonsToRegister.Name" class="form-control" />
                    </div>
                    <p> </p>
                    <div class="form-group row">
                        <div class="col-2">
                            <input type="submit" class="btn btn-info w-75" value="Обнови" />
                        </div>
                        <div class="col-2">
                            <a asp-action="Index" class="btn btn-danger w-75">Назад</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}